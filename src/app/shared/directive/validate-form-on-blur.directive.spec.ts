import { ValidateFormOnBlurDirective } from './validate-form-on-blur.directive';

describe('ValidateFormOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateFormOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
